name: deploy-branch

on:
  workflow_dispatch:

jobs:

  deploy_branch:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: checkout branch for build
        uses: actions/checkout@v3

      - name: install imagemagick
        run: |
          sudo apt install -y imagemagick

      - name: set up py3.8
        uses: actions/setup-python@v4
        with:
          python-version: 3.8

      - name: set up poetry
        uses: snok/install-poetry@v1.3.1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
          installer-parallel: true

      - name: load cached venv
        uses: actions/cache@v3
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('**/poetry.lock') }}

      - name: install dependencies
        run: |
          poetry install --no-interaction --no-root

      - name: build the book
        run: |
          export DWAVE_TOKEN="${{ secrets.DWAVE_TOKEN }}"
          poetry run jupyter-book toc migrate /home/runner/work/qmlcourse/qmlcourse/qmlcourse/_toc.yml -o /home/runner/work/qmlcourse/qmlcourse/qmlcourse/_toc.yml

          poetry run jupyter-book build ./qmlcourse

      - name: update publish dir to `web-page-stage`
        uses: peaceiris/actions-gh-pages@v3.8.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./qmlcourse/_build/html/
          publish_branch: web-page-stage

      - name: checkout to `web-page-stage` branch & create folder for latex
        uses: actions/checkout@v3
        with:
          ref: web-page-stage
      - run: |
          mkdir ./latex/

      - name: checkout to branch where it was run
        uses: actions/checkout@v3

      - name: build latex files
        run: |
          export DWAVE_TOKEN="${{ secrets.DWAVE_TOKEN }}"

          poetry run jupyter-book build ./qmlcourse --builder latex

      - name: update latex dir to `web-page-stage`
        uses: peaceiris/actions-gh-pages@v3.8.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./qmlcourse/_build/html/
          destination_dir: ./latex/
          publish_branch: web-page-stage
