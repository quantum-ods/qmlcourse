name: test-makefile

on:
  workflow_dispatch:

jobs:

  check-makefile:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: checkout branch for build
        uses: actions/checkout@v3

      - name: install dependencies
        run: make install-${{ matrix.os }}

      # - name: export poetry
      #   if: matrix.os == 'ubuntu-latest'
      #   run: |
      #     export PATH="$HOME/.poetry/bin:$PATH"
      #     poetry run jupyter-book toc migrate /home/runner/work/qmlcourse/qmlcourse/qmlcourse/_toc.yml -o /home/runner/work/qmlcourse/qmlcourse/qmlcourse/_toc.yml

      # - name: export poetry
      #   if: matrix.os == 'macos-latest'
      #     poetry run jupyter-book toc migrate /Users/runner/work/qmlcourse/qmlcourse/qmlcourse/_toc.yml -o /Users/runner/work/qmlcourse/qmlcourse/qmlcourse/_toc.yml

      - name: build the book
        env:
          DWAVE_TOKEN: ${{ secrets.DWAVE_TOKEN }}
          PATH: "$HOME/.poetry/bin:$PATH"
        run: |
          poetry run jupyter-book toc migrate ./qmlcourse/_toc.yml -o ./qmlcourse/_toc.yml
          make build-linux-macos
